{
  "name": "stimulus-bootstrap-behaviors",
  "version": "1.0.0",
  "description": "Stimulus controllers for Bootstrap interfaces, without Bootstrap JavaScript.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
